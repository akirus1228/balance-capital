import * from '../images/dark';
import * from '../images/light';

interface ThemeImageProps {
    image: string
}

export const ThemeImage = (props: ThemeImageProps): JSX.Element => {

    return(
        <img src={} alt="" />
    )
}